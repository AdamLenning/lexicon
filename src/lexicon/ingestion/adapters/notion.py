"""Notion adapter — requires NOTION_TOKEN and page IDs. v0 scaffold (v0.3)."""

from __future__ import annotations


async def fetch(page_ids: list[str], token: str) -> list[str]:
    raise NotImplementedError("v0.3")
