{
  "_comment": "Drop this into ~/Library/Application Support/Claude/claude_desktop_config.json (macOS). See README for other OS paths. Restart Claude Desktop after saving.",
  "mcpServers": {
    "lexicon": {
      "command": "lexicon-mcp",
      "env": {
        "LEXICON_DATABASE_URL": "postgresql://lexicon:lexicon@localhost:5433/lexicon"
      }
    }
  }
}
